;;
;; syntax argument
;;

;;;  | expression

rule Arg_ok/expression:
  p C |- expression : expressionIR `@ typeIR
  -- Expr_ok: p C |- expression : expressionIR
  -- if _ `( typeIR `; _) = expressionIR

;;;  | name `= expression

;;;  | `_

;;;  | name `= `_

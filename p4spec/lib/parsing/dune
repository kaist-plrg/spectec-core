(library
 (name parsing)
 (libraries util domain xl il)
 (modules source context extract parser value lexer ast_utils pp parse))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain))

(library
 (name parsing)
 (libraries util domain xl il core_unix)
 (modules source context extract parser value lexer preprocessor ast_utils pp parse))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain))

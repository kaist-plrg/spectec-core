(menhir
  (modules parser)
  (flags --no-stdlib --explain))

(ocamllex
  (modules lexer))

(library
  (name frontend) 
  (libraries syntax core core_kernel core_unix)
  (modules lexer parser preprocessor context parse))

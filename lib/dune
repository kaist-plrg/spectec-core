(menhir
  (modules parser)
  (flags --no-stdlib --explain))

(ocamllex
  (modules lexer))

(library
  (name p4cherry) 
  (libraries core core_kernel core_unix)
  (modules
    ast info name number text
    context lexer parser util))

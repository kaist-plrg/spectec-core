def $unop(unop, val) : val

def $binop(binop, val, val) : val

def $cast(val, rtype) : val

def $unpack(val) : num

def $bitslice(val, val, val) : val

def $default(rtype) : val

def $align_args(param*, arg*) : (param*, expr*)

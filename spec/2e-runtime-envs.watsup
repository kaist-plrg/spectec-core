syntax venv = map(id, val)
syntax tenv = map(id, typ)
syntax tdenv = map(tid, typdef)
syntax fdenv = map(fid, funcdef)
syntax cdenv = map(fid, consdef)

def $find_env(syntax K, syntax V, map(K, V), K) : V  hint(show %3#`[%4])
